--- accounting.py
+++ accounting.py
@@ -136,12 +136,12 @@
 
     return ceilometerclient.client.get_client(
         '2',
-        os_username = 'traffic-metering',
-        os_password = 'Aiwoofe7',
-        os_tenant_name = 'openstack',
-        os_auth_url = 'https://api.ostst.wgtn.cat-it.co.nz:5000/v2.0',
-        os_region_name = 'test-1',
-        insecure = True,
+        os_username = os.getenv('OS_USERNAME'),
+        os_password = os.getenv('OS_PASSWORD'),
+        os_tenant_name = os.getenv('OS_TENANT_NAME'),
+        os_auth_url = os.getenv('OS_AUTH_URL'),
+        os_region_name = region,
+        insecure = os.getenv('OS_INSECURE'),
     )
 
 def _neutron_floating_ip_list(clients):
